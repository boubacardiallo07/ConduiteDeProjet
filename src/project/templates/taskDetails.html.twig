{% block head %}
	<title>Kanban Table</title>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
	<style>
		.card.draggable {
			margin-bottom: 1rem;
			cursor: grab;
		}

		.droppable {
			background-color: var(--success);
			min-height: 120px;
			margin-bottom: 1rem;
		}
	</style>
{% endblock %}

{% block body %}


</li>

<li class="list-group-item"></li></div><div class="card text-center">
<div class="card-header">
	<strong>Titre:</strong>
	{{task.getTitle}}</div>
<div class="card-body">
	<h6>
		<strong>Priorité:</strong>
		{{task.getColor}}</h6>
	<h6 class="card-title">
		<strong>Assignation:</strong>
		{% for user in task.getAssignation()%}
			{{user.getUsername}}
		{% endfor %}
	</h6>
	<p class="card-text">
		<strong>Description:</strong>
		{{task.getDescription}}</p>
	<p>
		<strong>Estimation de la durée:</strong>
		{{task.getEstimation}}</p>
	<a class="btn btn-primary" href="{{ path ('app_editTask', {'projectId': project.getId(), 'sprintId': sprint.getId(), 'columnId' : kanbanColumn.getId(), 'taskId': task.getId() })}}">Editer</a>

	<a class="btn btn-primary" href="{{ path ('app_view_tab', {'projectId': project.getId(), 'sprintId': sprint.getId()})}}">Retour</a>

</div>
<div class="card-footer text-muted">
	<strong>Date de Création:</strong>
	{{task.getCreationDate.format('d/m/Y H:i:s')}}<br><strong>Mis à jour de la colonne:</strong>
	{{task.getColumnUpdate}}</li></div></div>{% endblock %}
